<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_presupuesto_venta_list" model="ir.actions.act_window">
        <field name="name">Presupuesto de Venta</field>
        <field name="res_model">presupuesto.venta</field>
        <field name="view_mode">tree,kanban,form</field>
    </record>

    <menuitem id="menu_presupuesto_venta_list"
        action="action_presupuesto_venta_list"
        parent="account.menu_finance_entries_management"/>

    <record id="presupuesto_venta_form" model="ir.ui.view">
        <field name="name">Presupuesto.venta.form</field>
        <field name="model">presupuesto.venta</field>
        <field name="arch" type="xml">
            <form string="Presupuesto de Venta" class="o_purchase_order">
                <header>
                    <button name="calcular" type="object" string="Calcular" states="draft"/>
                    <button name="excel" type="object" string="Excel" states="draft"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,sent,purchase" readonly="1"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <span class="o_form_label" attrs="{'invisible': [('state','not in',('draft','sent'))]}">Borrador </span>
                        <span class="o_form_label" attrs="{'invisible': [('state','in',('draft','sent'))]}">Presupuesto de Venta</span>
                        <h1>
                            <field name="priority" widget="priority" class="mr-3"/>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="id" invisible="1"/>
                            <field name="date_start" attrs="{'invisible': [('state','in',('purchase','done'))]}"/>
                            <field name="date_end" attrs="{'invisible': [('state','in',('purchase','done'))]}"/>
                        </group>
                        <group>
                            <field name="archivo" readonly="1" filename="name"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Products" name="products">
                            <!-- <field name="tax_country_id" invisible="1"/> -->
                            <field name="order_line"
                                widget="section_and_note_one2many"
                                mode="tree,kanban"
                                context="{'default_state': 'draft'}"
                                attrs="{'readonly': [('state', 'in', ('done', 'cancel'))]}">
                                <tree string="Lineas de Presupuesto Venta" editable="bottom">
                                    <field name="state" invisible="1"/>
                                    <field name="display_type" invisible="1"/>
                                    <field name="sequence" widget="handle"/>
                                    <field
                                        name="product_id"
                                        attrs="{
                                            'readonly': [('state', 'in', ('purchase', 'to approve','done', 'cancel'))],
                                            'required': [('display_type', '=', False)],
                                        }"
                                        context="{'company_id': parent.company_id}"
                                        force_save="1" domain="[('sale_ok', '=', True), '|', ('company_id', '=', False), ('company_id', '=', parent.company_id)]"/>
                                    <field name="name" widget="section_and_note_text"/>
                                    <field name="date_start" optional="show"/>
                                    <field name="date_end" optional="show"/>
                                    <field name="product_qty" optional="show"/>
                                    <field name="price_unit" optional="show"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Otra Informacion" name="presupesto_venta_otra_informacion">
                            <group>
                                <group name="other_info">
                                    <field name="user_id" domain="[('share', '=', False)]" widget="many2one_avatar_user"/>
                                    <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                                    <field name="currency_id" groups="base.group_multi_currency" force_save="1"/>
                                    <field name="date" attrs="{'invisible': [('state','in',('purchase','done'))]}"/>
                                </group>
                            </group>
                        </page>                            
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="presupuesto_venta_tree" model="ir.ui.view">
        <field name="name">presupuesto.venta.tree</field>
        <field name="model">presupuesto.venta</field>
        <field name="priority" eval="1"/>
        <field name="arch" type="xml">
            <tree string="Presupuesto de Venta" decoration-bf="message_unread==True"
                    decoration-muted="state=='cancel'" sample="1">
                <field name="priority" optional="show" widget="priority" nolabel="1"/>
                <field name="message_unread" invisible="1"/>
                <field name="name" string="Reference" readonly="1" decoration-info="state in ('draft','sent')"/>
                <field name="date" invisible="not context.get('quotation_only', False)" optional="show"/>
                <field name="company_id" readonly="1" options="{'no_create': True}"
                    groups="base.group_multi_company" optional="show"/>
                <field name="user_id" optional="show"/>
                <field name="currency_id" invisible="1"/>
                <field name="state" optional="show"/>
                <field name="activity_exception_decoration" widget="activity_exception"/>
                <field name="date_start" optional="show"/>
                <field name="date_end" optional="show"/>
            </tree>
        </field>
    </record>
</odoo>